---
- name: Implementing play scope variables
  hosts: all
  vars:
    a: tomcat10
    b: present
    c: yes
  tasks:
    - name: Install/uninstall s/w applications
      apt:
        name: "{{software_namea}}"
        state: "{{software_state}}"
        update_cache: "{{repo}}"

