---
- name: Create user on all servers
  hosts: appserver
  tasks:
    - name: User creation
      user:
        name: Anu
        password: intelliqit
        uid: 1234
        home: /home/Anu
        comment: "a normal user"
        shell: /bin/bash

...
